{"classData":[{"comment":"/**\n * Pure, side-effect-free checker for STRICT merge-readiness of a pull request.\n *\n * `reviewDecision=APPROVED` + green checks are necessary but NOT sufficient: GitHub flattens\n * `reviewDecision` to APPROVED as soon as it has enough formal review state, even while an\n * explicitly-requested reviewer is still outstanding (`reviewRequests` non-empty). The author/operator's\n * requested-reviewer set is an explicit contract — a non-empty `reviewRequests` blocks strict\n * merge-readiness until each request is disposed (a formal APPROVED / CHANGES_REQUESTED review, a\n * visible step-out, or an author/operator unrequest). An A2A peer approval does NOT clear a\n * requested-reviewer slot. Human-only merge authority is unchanged; this only validates the *claim*\n * that a PR is strict-merge-ready, so a stale reviewer contract is not reported as a green surface.\n *\n * @module Neo.ai.scripts.lifecycle.validateMergeReady\n */","meta":{"filename":"validateMergeReady.mjs","lineno":1,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/lifecycle","code":{}},"description":"<p>Pure, side-effect-free checker for STRICT merge-readiness of a pull request.</p>\n<p><code>reviewDecision=APPROVED</code> + green checks are necessary but NOT sufficient: GitHub flattens\n<code>reviewDecision</code> to APPROVED as soon as it has enough formal review state, even while an\nexplicitly-requested reviewer is still outstanding (<code>reviewRequests</code> non-empty). The author/operator&#39;s\nrequested-reviewer set is an explicit contract — a non-empty <code>reviewRequests</code> blocks strict\nmerge-readiness until each request is disposed (a formal APPROVED / CHANGES_REQUESTED review, a\nvisible step-out, or an author/operator unrequest). An A2A peer approval does NOT clear a\nrequested-reviewer slot. Human-only merge authority is unchanged; this only validates the <em>claim</em>\nthat a PR is strict-merge-ready, so a stale reviewer contract is not reported as a green surface.</p>\n","kind":"module","name":"validateMergeReady","memberof":"Neo.ai.scripts.lifecycle","longname":"Neo.ai.scripts.lifecycle.module:validateMergeReady","scope":"static","$longname":"Neo.ai.scripts.lifecycle.module:validateMergeReady","$kind":"module","id":8380,"neoClassName":"Neo.ai.scripts.lifecycle.validateMergeReady"},{"comment":"/**\n * The ONLY `mergeStateStatus` values that confirm a PR is mergeable. An allowlist, NOT a denylist:\n * any other state — `DIRTY`/`BEHIND`/`BLOCKED` (a conflict / stale base) or `UNKNOWN` (GitHub has not\n * computed mergeability yet) — fails closed, since an unconfirmed state cannot certify strict readiness.\n * `UNSTABLE` is admitted (it is mergeable; the separate `checksGreen` gate covers CI).\n * @type {String[]}\n */","meta":{"range":[1356,1410],"filename":"validateMergeReady.mjs","lineno":23,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/lifecycle","code":{"id":"astnode100080474","name":"exports.MERGEABLE_STATES","type":"VariableDeclaration"}},"description":"<p>The ONLY <code>mergeStateStatus</code> values that confirm a PR is mergeable. An allowlist, NOT a denylist:\nany other state — <code>DIRTY</code>/<code>BEHIND</code>/<code>BLOCKED</code> (a conflict / stale base) or <code>UNKNOWN</code> (GitHub has not\ncomputed mergeability yet) — fails closed, since an unconfirmed state cannot certify strict readiness.\n<code>UNSTABLE</code> is admitted (it is mergeable; the separate <code>checksGreen</code> gate covers CI).</p>\n","type":{"names":["Array.<String>"]},"name":"MERGEABLE_STATES","longname":"Neo.ai.scripts.lifecycle.module:validateMergeReady.MERGEABLE_STATES","kind":"constant","memberof":"Neo.ai.scripts.lifecycle.module:validateMergeReady","scope":"static","$longname":"Neo.ai.scripts.lifecycle.module:validateMergeReady.MERGEABLE_STATES","$kind":"constant","id":8381,"neoClassName":"Neo.ai.scripts.lifecycle.validateMergeReady"},{"comment":"/**\n * @summary Validates whether a PR is STRICT merge-ready against the full review/merge contract.\n *\n * Rules (all must hold for `strictMergeReady`):\n *  1. `reviewDecision === 'APPROVED'`.\n *  2. `checksGreen === true` (all required CI checks pass).\n *  3. `mergeStateStatus` is fetched AND a confirmed-mergeable state (allowlist `CLEAN`/`UNSTABLE`); `UNKNOWN`\n *     (mergeability not yet computed) and DIRTY/BEHIND/BLOCKED all fail closed.\n *  4. `reviewRequests` is fetched AND every explicitly-requested reviewer is disposed — i.e. `reviewRequests`\n *     minus `disposedReviewers` is empty (the reviewer-contract gate).\n *\n * Fail-CLOSED contract: `checksGreen`, `mergeStateStatus`, and `reviewRequests` that were NOT fetched\n * (`undefined`) each block readiness — an un-queried field cannot certify a green surface, and an\n * omitted `reviewRequests` must never be read as \"no outstanding reviewers\". Pass `reviewRequests: []`\n * to assert it was fetched-and-empty.\n *\n * @param {Object} [pr={}]\n * @param {String} [pr.reviewDecision] GitHub flattened review decision (`APPROVED` | `CHANGES_REQUESTED` | `REVIEW_REQUIRED` | null).\n * @param {Boolean} [pr.checksGreen] True when all required CI checks pass; `undefined` (not fetched) fails closed.\n * @param {String} [pr.mergeStateStatus] GitHub mergeStateStatus (`CLEAN` | `UNSTABLE` | `DIRTY` | `BEHIND` | `BLOCKED` | ...); `undefined` (not fetched) fails closed.\n * @param {String[]} [pr.reviewRequests] Logins of still-requested reviewers (the explicit author/operator contract); `undefined` (not fetched) fails closed, `[]` asserts fetched-and-empty.\n * @param {String[]} [pr.disposedReviewers] Requested reviewers already disposed (formal review / visible step-out / unrequest).\n * @returns {{strictMergeReady: Boolean, blockers: String[]}}\n */","meta":{"range":[3223,5079],"filename":"validateMergeReady.mjs","lineno":49,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/lifecycle","code":{"id":"astnode100080481","name":"exports.validateMergeReady","type":"FunctionDeclaration","paramnames":["pr"]}},"summary":"Validates whether a PR is STRICT merge-ready against the full review/merge contract.\n\nRules (all must hold for `strictMergeReady`):\n 1. `reviewDecision === 'APPROVED'`.\n 2. `checksGreen === true` (all required CI checks pass).\n 3. `mergeStateStatus` is fetched AND a confirmed-mergeable state (allowlist `CLEAN`/`UNSTABLE`); `UNKNOWN`\n    (mergeability not yet computed) and DIRTY/BEHIND/BLOCKED all fail closed.\n 4. `reviewRequests` is fetched AND every explicitly-requested reviewer is disposed — i.e. `reviewRequests`\n    minus `disposedReviewers` is empty (the reviewer-contract gate).\n\nFail-CLOSED contract: `checksGreen`, `mergeStateStatus`, and `reviewRequests` that were NOT fetched\n(`undefined`) each block readiness — an un-queried field cannot certify a green surface, and an\nomitted `reviewRequests` must never be read as \"no outstanding reviewers\". Pass `reviewRequests: []`\nto assert it was fetched-and-empty.","params":[{"type":{"names":["Object"]},"optional":true,"defaultvalue":"{}","name":"pr"},{"type":{"names":["String"]},"optional":true,"description":"<p>GitHub flattened review decision (<code>APPROVED</code> | <code>CHANGES_REQUESTED</code> | <code>REVIEW_REQUIRED</code> | null).</p>\n","name":"pr.reviewDecision"},{"type":{"names":["Boolean"]},"optional":true,"description":"<p>True when all required CI checks pass; <code>undefined</code> (not fetched) fails closed.</p>\n","name":"pr.checksGreen"},{"type":{"names":["String"]},"optional":true,"description":"<p>GitHub mergeStateStatus (<code>CLEAN</code> | <code>UNSTABLE</code> | <code>DIRTY</code> | <code>BEHIND</code> | <code>BLOCKED</code> | ...); <code>undefined</code> (not fetched) fails closed.</p>\n","name":"pr.mergeStateStatus"},{"type":{"names":["Array.<String>"]},"optional":true,"description":"<p>Logins of still-requested reviewers (the explicit author/operator contract); <code>undefined</code> (not fetched) fails closed, <code>[]</code> asserts fetched-and-empty.</p>\n","name":"pr.reviewRequests"},{"type":{"names":["Array.<String>"]},"optional":true,"description":"<p>Requested reviewers already disposed (formal review / visible step-out / unrequest).</p>\n","name":"pr.disposedReviewers"}],"returns":[{"type":{"names":["Object"]}}],"name":"validateMergeReady","longname":"Neo.ai.scripts.lifecycle.module:validateMergeReady.validateMergeReady","kind":"function","memberof":"Neo.ai.scripts.lifecycle.module:validateMergeReady","scope":"static","$longname":"Neo.ai.scripts.lifecycle.module:validateMergeReady.validateMergeReady","$kind":"method","id":8382,"neoClassName":"Neo.ai.scripts.lifecycle.validateMergeReady"}]}