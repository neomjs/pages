import a from"../../core/Base.mjs";export default Neo.setupClass(class extends a{static config={className:"Neo.data.normalizer.Base",ntype:"normalizer-base"};normalize(a){return{data:a,totalCount:Array.isArray(a)?a.length:1}}});