import e from"../core/EffectManager.mjs";const o=Symbol.for("pendingDomEvents");export function useEvent(t,n,r){const s=e.getActiveEffect(),c=s?.componentId;if(!c)throw new Error("useEvent must be called from within a functional component's createVdom method.");e.pause();const f=Neo.getComponent(c);if(e.resume(),!f)throw new Error(`Component with id ${c} not found for useEvent hook.`);f[o]??=[],f[o].push({[t]:n,delegate:r,scope:f})}export default useEvent;