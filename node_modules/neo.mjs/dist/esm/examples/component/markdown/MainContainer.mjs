import e from"../../../src/button/Base.mjs";import n from"../../../src/component/markdown/Component.mjs";import t from"../../../src/toolbar/Base.mjs";import o from"../../../src/container/Viewport.mjs";const a="# Streaming Markdown, VDOM-native\n\nThis component parses markdown into **pure vdom subtrees** — no `innerHTML` at any stage.\nSettled blocks keep *reference-identical* ids across appends, so the differ no-ops them and\nstreaming stays cheap. Docs live in the [neo repo](https://github.com/neomjs/neo).\n\n## Why transcripts need this\n\n- Agent responses stream as markdown\n- Untrusted content renders inert by construction\n- Appends produce insert-only tail batches\n- The open tail block grows via updateVtext\n\n1. Stable ids first\n2. Security defaults second\n3. Feature breadth last\n\n> The ledger is the oracle — liveness is never DOM-probed.\n\n```js\nconst parser = new MarkdownParser({idPrefix: 'demo'});\nconst blocks = parser.update(source);\n```\n\n| Surface | Layer |\n|---|---|\n| Parser | App Worker |\n| Deltas | Main |\n\n---\n\nRaw HTML stays harmless: <script>alert('nope')<\/script> and hostile links\nlike [click me](javascript:alert(1)) render as plain text.\n";export default Neo.setupClass(class extends o{static config={className:"Neo.examples.component.markdown.MainContainer",cls:["neo-examples-markdown-viewport"],layout:{ntype:"vbox",align:"stretch"},items:[{module:t,flex:"none",items:[{module:e,handler:"up.onStreamButtonClick",iconCls:"fa fa-play",text:"Stream demo"},{module:e,handler:"up.onMarathonButtonClick",iconCls:"fa fa-person-running",text:"Stream marathon (40×)"},{module:e,handler:"up.onRenderButtonClick",iconCls:"fa fa-bolt",text:"Render instantly"},{module:e,handler:"up.onResetButtonClick",iconCls:"fa fa-rotate-left",text:"Reset"}]},{module:n,flex:1,reference:"markdown-output",style:{overflow:"auto",padding:"1em"}}]};streamRun=0;async onStreamButtonClick(){let e=this,n=e.getReference("markdown-output"),t=0,o=++e.streamRun;for(n.value=null;t<a.length&&e.streamRun===o;)t=Math.min(t+24,a.length),n.value=a.slice(0,t),await e.timeout(50)}async onMarathonButtonClick(){let e=this,n=e.getReference("markdown-output"),t=Array.from({length:40},(e,n)=>`# Marathon section ${n+1}\n\n${a}`).join("\n\n"),o=0,r=++e.streamRun;for(n.value=null;o<t.length&&e.streamRun===r;)o=Math.min(o+400,t.length),n.value=t.slice(0,o),await e.timeout(20)}onRenderButtonClick(){this.streamRun++,this.getReference("markdown-output").value=a}onResetButtonClick(){this.streamRun++,this.getReference("markdown-output").value=null}});