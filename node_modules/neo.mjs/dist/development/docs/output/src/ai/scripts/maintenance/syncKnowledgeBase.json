{"classData":[{"comment":"/**\n * @module ai/scripts/maintenance/syncKnowledgeBase\n *\n * Progress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\n * the orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\n * deferral therefore records `skipped` (not a false-green `completed` that refreshes\n * `lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.\n */","meta":{"filename":"syncKnowledgeBase.mjs","lineno":15,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/maintenance","code":{}},"kind":"module","name":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.","longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:","$longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:","$kind":"module","id":8781,"neoClassName":"Neo.ai.scripts.maintenance.syncKnowledgeBase"},{"comment":"/**\n * Logs supervised-child progress to STDERR (STDOUT carries the single structured outcome JSON),\n * prefixed `[INFO]` so the orchestrator's `ProcessSupervisorService` classifies it as INFO. Its\n * `getChildLogLevel` defaults UNPREFIXED child stderr to ERROR, which otherwise mis-stamps routine\n * progress as errors and camouflages real failures. Genuine failures stay on raw `console.error`\n * (unprefixed → ERROR).\n * @param {...*} args\n */","meta":{"range":[2244,2303],"filename":"syncKnowledgeBase.mjs","lineno":32,"columnno":6,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/maintenance","code":{"id":"astnode100140795","name":"logProgress","type":"ArrowFunctionExpression"}},"description":"<p>Logs supervised-child progress to STDERR (STDOUT carries the single structured outcome JSON),\nprefixed <code>[INFO]</code> so the orchestrator&#39;s <code>ProcessSupervisorService</code> classifies it as INFO. Its\n<code>getChildLogLevel</code> defaults UNPREFIXED child stderr to ERROR, which otherwise mis-stamps routine\nprogress as errors and camouflages real failures. Genuine failures stay on raw <code>console.error</code>\n(unprefixed → ERROR).</p>\n","params":[{"type":{"names":["*"]},"variable":true,"name":"args"}],"name":"logProgress","longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:~logProgress","kind":"function","scope":"inner","memberof":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:","$longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:~logProgress","$kind":"method","id":8782,"neoClassName":"Neo.ai.scripts.maintenance.syncKnowledgeBase"},{"comment":"/**\n * Builds the cooperative heavy-maintenance-lease yield predicate from a lease acquisition descriptor.\n * Reads the active-hold fairness bound from the AiConfig leaf at the use site (per call, never module-load-captured). Exported so a\n * boundary test can assert this script reads the correct config branch — `orchestrator.heavyMaintenance`\n * (which holds `maxActiveHoldMs`), NOT the sibling `orchestrator.heavyMaintenanceLease` (which holds only\n * `staleAfterMs`). The direct VectorService embed seam cannot prove the script-level config wiring.\n * @param {{lease: Object}} acquisition The `withHeavyMaintenanceLease` descriptor (`{status, acquired, lease}`).\n * @returns {Function} A zero-arg predicate the shadow-swap embed loop consults between batches.\n */","meta":{"range":[3075,3291],"filename":"syncKnowledgeBase.mjs","lineno":43,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/maintenance","code":{"id":"astnode100140807","name":"exports.buildLeaseYieldPredicate","type":"FunctionDeclaration","paramnames":["acquisition"]}},"description":"<p>Builds the cooperative heavy-maintenance-lease yield predicate from a lease acquisition descriptor.\nReads the active-hold fairness bound from the AiConfig leaf at the use site (per call, never module-load-captured). Exported so a\nboundary test can assert this script reads the correct config branch — <code>orchestrator.heavyMaintenance</code>\n(which holds <code>maxActiveHoldMs</code>), NOT the sibling <code>orchestrator.heavyMaintenanceLease</code> (which holds only\n<code>staleAfterMs</code>). The direct VectorService embed seam cannot prove the script-level config wiring.</p>\n","params":[{"type":{"names":["Object"]},"description":"<p>The <code>withHeavyMaintenanceLease</code> descriptor (<code>{status, acquired, lease}</code>).</p>\n","name":"acquisition"}],"returns":[{"type":{"names":["function"]},"description":"A zero-arg predicate the shadow-swap embed loop consults between batches."}],"name":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.buildLeaseYieldPredicate","longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.buildLeaseYieldPredicate","kind":"function","memberof":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:","$longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.buildLeaseYieldPredicate","$kind":"method","id":8783,"neoClassName":"Neo.ai.scripts.maintenance.syncKnowledgeBase"},{"comment":"/**\n * Maps the `withHeavyMaintenanceLease` outcome to the structured stdout envelope that\n * `ProcessSupervisorService.classifySuccessfulChildOutcome` and `PrimaryRepoSyncService.runKbSync` consume.\n * A lease-HELD run and a cooperative lease-YIELD are both PARTIAL (no full sync completed), so both carry\n * `{deferred: true, reason}` — the consumers record `skipped`, never a false-green `completed` that would\n * refresh kbSync's lastSuccessAt. A real run carries `{deferred: false, ...counts}`. Pure (no I/O) so it is\n * unit-testable without the script's Neo bootstrap.\n * @param {Object} outcome The `withHeavyMaintenanceLease` return (`{status, result, lease}`).\n * @returns {Object} The structured stdout object.\n */","meta":{"range":[4019,4576],"filename":"syncKnowledgeBase.mjs","lineno":59,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/maintenance","code":{"id":"astnode100140828","name":"exports.classifyKbSyncOutcome","type":"FunctionDeclaration","paramnames":["outcome"]}},"description":"<p>Maps the <code>withHeavyMaintenanceLease</code> outcome to the structured stdout envelope that\n<code>ProcessSupervisorService.classifySuccessfulChildOutcome</code> and <code>PrimaryRepoSyncService.runKbSync</code> consume.\nA lease-HELD run and a cooperative lease-YIELD are both PARTIAL (no full sync completed), so both carry\n<code>{deferred: true, reason}</code> — the consumers record <code>skipped</code>, never a false-green <code>completed</code> that would\nrefresh kbSync&#39;s lastSuccessAt. A real run carries <code>{deferred: false, ...counts}</code>. Pure (no I/O) so it is\nunit-testable without the script&#39;s Neo bootstrap.</p>\n","params":[{"type":{"names":["Object"]},"description":"<p>The <code>withHeavyMaintenanceLease</code> return (<code>{status, result, lease}</code>).</p>\n","name":"outcome"}],"returns":[{"type":{"names":["Object"]},"description":"The structured stdout object."}],"name":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.classifyKbSyncOutcome","longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.classifyKbSyncOutcome","kind":"function","memberof":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:","$longname":"ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.ai/scripts/maintenance/syncKnowledgeBase\n\nProgress/diagnostic logs go to STDERR; STDOUT carries exactly one structured outcome JSON\nthe orchestrator's `ProcessSupervisorService` captures (`captureStdoutJson`). A lease-held\ndeferral therefore records `skipped` (not a false-green `completed` that refreshes\n`lastSuccessAt`) — the kb-sync half of the deferred-as-completed fix.module:.classifyKbSyncOutcome","$kind":"method","id":8784,"neoClassName":"Neo.ai.scripts.maintenance.syncKnowledgeBase"}]}