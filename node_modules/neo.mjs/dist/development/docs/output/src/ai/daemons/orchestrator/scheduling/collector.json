{"classData":[{"comment":"/**\n * Pure collector for the Orchestrator scheduling pipeline.\n *\n * Iterates a registry of coordinator descriptors, projects each to a due-trigger via the\n * descriptor's `getDueTask(context)` adapter, normalizes the trigger shape, and attaches\n * the descriptor as metadata so downstream `pickNextCandidate` can apply policy rules.\n *\n * The collector is intentionally policy-blind: it contains no task-kind,\n * maintenance-class, or deployment-profile branching. Eligibility decisions live in\n * registry descriptor adapters via caller-provided getter values; cross-candidate\n * policy decisions live in `pickNextCandidate`. The collector itself is pure\n * iteration + normalization + metadata attachment.\n *\n * Descriptor failures are reported as data, not written directly to Orchestrator\n * state. The caller owns state-mutating health reporting, keeping this module\n * observably pure.\n *\n * @param {Object} options\n * @param {Array<Object>} options.registry Frozen task-descriptor registry (see `registry.mjs`).\n * @param {Object} options.context Uniform context passed to every descriptor's `getDueTask`.\n *   Must include `state`, `now`, `intervals`, `enables`, `hooks` (descriptor-specific\n *   destructure shape lives inside each descriptor's adapter closure).\n * @returns {{candidates: Array<Object>, errors: Array<Object>}} Due candidates and per-task\n *   errors, both as plain data structures. No side effects.\n */","meta":{"range":[1432,2038],"filename":"collector.mjs","lineno":26,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/daemons/orchestrator/scheduling","code":{"id":"astnode100092184","name":"exports.collectDueCandidates","type":"FunctionDeclaration","paramnames":[""]}},"description":"<p>Pure collector for the Orchestrator scheduling pipeline.</p>\n<p>Iterates a registry of coordinator descriptors, projects each to a due-trigger via the\ndescriptor&#39;s <code>getDueTask(context)</code> adapter, normalizes the trigger shape, and attaches\nthe descriptor as metadata so downstream <code>pickNextCandidate</code> can apply policy rules.</p>\n<p>The collector is intentionally policy-blind: it contains no task-kind,\nmaintenance-class, or deployment-profile branching. Eligibility decisions live in\nregistry descriptor adapters via caller-provided getter values; cross-candidate\npolicy decisions live in <code>pickNextCandidate</code>. The collector itself is pure\niteration + normalization + metadata attachment.</p>\n<p>Descriptor failures are reported as data, not written directly to Orchestrator\nstate. The caller owns state-mutating health reporting, keeping this module\nobservably pure.</p>\n","params":[{"type":{"names":["Object"]},"name":"options"},{"type":{"names":["Array.<Object>"]},"description":"<p>Frozen task-descriptor registry (see <code>registry.mjs</code>).</p>\n","name":"options.registry"},{"type":{"names":["Object"]},"description":"<p>Uniform context passed to every descriptor&#39;s <code>getDueTask</code>.\n  Must include <code>state</code>, <code>now</code>, <code>intervals</code>, <code>enables</code>, <code>hooks</code> (descriptor-specific\n  destructure shape lives inside each descriptor&#39;s adapter closure).</p>\n","name":"options.context"}],"returns":[{"type":{"names":["Object"]},"description":"Due candidates and per-task\n  errors, both as plain data structures. No side effects."}],"name":"collectDueCandidates","longname":"collectDueCandidates","kind":"function","scope":"global","$longname":"collectDueCandidates","$kind":"method","id":10150,"neoClassName":"Neo.ai.daemons.orchestrator.scheduling.collector"},{"comment":"/**\n * Normalizes a getDueTask return value to a uniform `{reason, onSuccess}` shape.\n *\n * Registry descriptors use object triggers, but this normalizer keeps the collector\n * tolerant of any future bare-boolean trigger.\n *\n * @param {Object|Boolean} trigger Raw trigger from `getDueTask`.\n * @returns {Object} Normalized `{reason, onSuccess?, ...originalFields}`.\n */","meta":{"range":[2410,2555],"filename":"collector.mjs","lineno":57,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/daemons/orchestrator/scheduling","code":{"id":"astnode100092255","name":"normalizeTrigger","type":"FunctionDeclaration","paramnames":["trigger"]}},"description":"<p>Normalizes a getDueTask return value to a uniform <code>{reason, onSuccess}</code> shape.</p>\n<p>Registry descriptors use object triggers, but this normalizer keeps the collector\ntolerant of any future bare-boolean trigger.</p>\n","params":[{"type":{"names":["Object","Boolean"]},"description":"<p>Raw trigger from <code>getDueTask</code>.</p>\n","name":"trigger"}],"returns":[{"type":{"names":["Object"]},"description":"Normalized `{reason, onSuccess?, ...originalFields}`."}],"name":"normalizeTrigger","longname":"normalizeTrigger","kind":"function","scope":"global","$longname":"normalizeTrigger","$kind":"method","id":10151,"neoClassName":"Neo.ai.daemons.orchestrator.scheduling.collector"}]}