{"classData":[{"comment":"/**\n * @module ai/daemons/orchestrator/services/dimensionConsistencyGatherer\n * @summary Orchestrator live-Chroma gatherer for the dimension-consistency detect signal — the audit +\n * scheduling half that `dimensionConsistencyDiagnosis` (detect-only, samples-injected) deliberately omits.\n *\n * It runs the pure, embedder-free `auditCollectionVectorDimensions` primitive across the supplied live\n * collections to produce the producer's per-collection samples, then feeds them to\n * `buildDimensionConsistencyDiagnosis` to emit a `recovery-diagnosis` (or `null` when every sampled\n * collection's vectors match the configured embedding dimension).\n *\n * Degrade-not-throw: the audit primitive returns a zero-count sample with an `error` field on probe failure\n * rather than throwing, so one unreachable collection never aborts the gather — the remaining collections'\n * mismatches still surface. The audit is a read-only `.get` and does NOT invoke the embedder, so this\n * gatherer is not gated by the embed canary. Pure-ish: the only I/O is the injected collections' reads.\n */","meta":{"filename":"dimensionConsistencyGatherer.mjs","lineno":1,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/daemons/orchestrator/services","code":{}},"kind":"module","name":"ai/daemons/orchestrator/services/dimensionConsistencyGatherer","summary":"Orchestrator live-Chroma gatherer for the dimension-consistency detect signal — the audit +\nscheduling half that `dimensionConsistencyDiagnosis` (detect-only, samples-injected) deliberately omits.\n\nIt runs the pure, embedder-free `auditCollectionVectorDimensions` primitive across the supplied live\ncollections to produce the producer's per-collection samples, then feeds them to\n`buildDimensionConsistencyDiagnosis` to emit a `recovery-diagnosis` (or `null` when every sampled\ncollection's vectors match the configured embedding dimension).\n\nDegrade-not-throw: the audit primitive returns a zero-count sample with an `error` field on probe failure\nrather than throwing, so one unreachable collection never aborts the gather — the remaining collections'\nmismatches still surface. The audit is a read-only `.get` and does NOT invoke the embedder, so this\ngatherer is not gated by the embed canary. Pure-ish: the only I/O is the injected collections' reads.","longname":"module:ai/daemons/orchestrator/services/dimensionConsistencyGatherer","$longname":"ai/daemons/orchestrator/services/dimensionConsistencyGatherer","$kind":"module","id":10672,"neoClassName":"Neo.ai.daemons.orchestrator.services.dimensionConsistencyGatherer"},{"comment":"/**\n * @summary Gathers per-collection vector-dimension samples from live Chroma collections and builds a\n * dimension-consistency `recovery-diagnosis`.\n *\n * @param {Object} options\n * @param {Array<{collection: Object, collectionName: String}>} [options.collections=[]] Live collections to audit.\n * @param {Number} options.expectedDimension The configured embedding dimension.\n * @param {String} options.serviceId The Memory Core compose-service identifier.\n * @param {Number} options.observedAt Epoch milliseconds when the audit is observed.\n * @param {Number} [options.sampleSize=100] Vectors to sample per collection.\n * @param {Function} [options.auditFn=auditCollectionVectorDimensions] The dimension-audit primitive — injectable for tests.\n * @returns {Promise<Object|null>} A `recovery-diagnosis` event when any collection has a dimension mismatch, else `null`.\n */","meta":{"range":[2152,2660],"filename":"dimensionConsistencyGatherer.mjs","lineno":33,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/daemons/orchestrator/services","code":{"id":"astnode100128864","name":"exports.gatherDimensionConsistencyDiagnosis","type":"FunctionDeclaration","paramnames":[""]}},"summary":"Gathers per-collection vector-dimension samples from live Chroma collections and builds a\ndimension-consistency `recovery-diagnosis`.","params":[{"type":{"names":["Object"]},"name":"options"},{"type":{"names":["Array.<{collection: Object, collectionName: String}>"]},"optional":true,"defaultvalue":"[]","description":"<p>Live collections to audit.</p>\n","name":"options.collections"},{"type":{"names":["Number"]},"description":"<p>The configured embedding dimension.</p>\n","name":"options.expectedDimension"},{"type":{"names":["String"]},"description":"<p>The Memory Core compose-service identifier.</p>\n","name":"options.serviceId"},{"type":{"names":["Number"]},"description":"<p>Epoch milliseconds when the audit is observed.</p>\n","name":"options.observedAt"},{"type":{"names":["Number"]},"optional":true,"defaultvalue":100,"description":"<p>Vectors to sample per collection.</p>\n","name":"options.sampleSize"},{"type":{"names":["function"]},"optional":true,"defaultvalue":"auditCollectionVectorDimensions","description":"<p>The dimension-audit primitive — injectable for tests.</p>\n","name":"options.auditFn"}],"returns":[{"type":{"names":["Promise.<(Object|null)>"]},"description":"A `recovery-diagnosis` event when any collection has a dimension mismatch, else `null`."}],"name":"gatherDimensionConsistencyDiagnosis","longname":"module:ai/daemons/orchestrator/services/dimensionConsistencyGatherer.gatherDimensionConsistencyDiagnosis","kind":"function","memberof":"module:ai/daemons/orchestrator/services/dimensionConsistencyGatherer","scope":"static","$longname":"ai/daemons/orchestrator/services/dimensionConsistencyGatherer.gatherDimensionConsistencyDiagnosis","$kind":"method","id":10673,"neoClassName":"Neo.ai.daemons.orchestrator.services.dimensionConsistencyGatherer"},{"comment":"/**\n * @summary Builds the live-Chroma dimension-consistency gatherer the data-integrity sweep schedules: it\n * resolves the Memory Core collections from the injected storage router and binds the producer config, then\n * returns a closure that runs {@link gatherDimensionConsistencyDiagnosis} over the resolved live collections.\n *\n * The live-collection resolution + config binding live here with the gatherer service (not in the\n * scheduling/orchestrator class): the orchestrator reads the AiConfig leaf at its use-site per the SSOT\n * discipline and injects the resolved `expectedDimension` + the `storageRouter`, keeping this module free of\n * config/Neo imports while owning the gather wiring. Injected `storageRouter` makes the factory testable in\n * isolation (no live Chroma).\n *\n * @param {Object} options\n * @param {Object} options.storageRouter The Memory Core storage router — supplies `ready()` +\n * `getMemoryCollection()` / `getSummaryCollection()`.\n * @param {Number} options.expectedDimension The configured embedding dimension (read at the orchestrator use-site).\n * @param {String} options.serviceId The Memory Core compose-service identifier.\n * @param {Function} [options.auditFn] The dimension-audit primitive — passed through to the gatherer; injectable for tests.\n * @returns {Function} `async (observedAt) => Promise<Object|null>` — a dimension-mismatch recovery-diagnosis or null.\n */","meta":{"range":[4074,5406],"filename":"dimensionConsistencyGatherer.mjs","lineno":69,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/daemons/orchestrator/services","code":{"id":"astnode100128930","name":"exports.createLiveDimensionConsistencyGatherer","type":"FunctionDeclaration","paramnames":[""]}},"summary":"Builds the live-Chroma dimension-consistency gatherer the data-integrity sweep schedules: it\nresolves the Memory Core collections from the injected storage router and binds the producer config, then\nreturns a closure that runs {@link gatherDimensionConsistencyDiagnosis} over the resolved live collections.\n\nThe live-collection resolution + config binding live here with the gatherer service (not in the\nscheduling/orchestrator class): the orchestrator reads the AiConfig leaf at its use-site per the SSOT\ndiscipline and injects the resolved `expectedDimension` + the `storageRouter`, keeping this module free of\nconfig/Neo imports while owning the gather wiring. Injected `storageRouter` makes the factory testable in\nisolation (no live Chroma).","params":[{"type":{"names":["Object"]},"name":"options"},{"type":{"names":["Object"]},"description":"<p>The Memory Core storage router — supplies <code>ready()</code> +\n<code>getMemoryCollection()</code> / <code>getSummaryCollection()</code>.</p>\n","name":"options.storageRouter"},{"type":{"names":["Number"]},"description":"<p>The configured embedding dimension (read at the orchestrator use-site).</p>\n","name":"options.expectedDimension"},{"type":{"names":["String"]},"description":"<p>The Memory Core compose-service identifier.</p>\n","name":"options.serviceId"},{"type":{"names":["function"]},"optional":true,"description":"<p>The dimension-audit primitive — passed through to the gatherer; injectable for tests.</p>\n","name":"options.auditFn"}],"returns":[{"type":{"names":["function"]},"description":"`async (observedAt) => Promise<Object|null>` — a dimension-mismatch recovery-diagnosis or null."}],"name":"createLiveDimensionConsistencyGatherer","longname":"module:ai/daemons/orchestrator/services/dimensionConsistencyGatherer.createLiveDimensionConsistencyGatherer","kind":"function","memberof":"module:ai/daemons/orchestrator/services/dimensionConsistencyGatherer","scope":"static","$longname":"ai/daemons/orchestrator/services/dimensionConsistencyGatherer.createLiveDimensionConsistencyGatherer","$kind":"method","id":10674,"neoClassName":"Neo.ai.daemons.orchestrator.services.dimensionConsistencyGatherer"}]}