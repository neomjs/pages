{"classData":[{"comment":"/**\n * @summary Pure rolling-window rate check for the `ask_knowledge_base` synthesis runaway breaker.\n *\n * Lives in its own module so the breaker is unit-testable in isolation — importing it does NOT\n * construct the `SearchService` singleton or read the shared `AiConfig` (no live-DB / config-bleed\n * surface). Generic + side-effect-free: callers pass the clock (`now`) and the cap explicitly.\n *\n * @param {Number[]} timestamps Prior call epoch-ms timestamps (the caller's rolling buffer).\n * @param {Number} now Current epoch-ms (injected for testability — never reads the clock itself).\n * @param {Number} maxPerMinute Cap within the rolling window.\n * @param {Number} [windowMs=60000] Rolling window width in ms.\n * @returns {{limited: Boolean, kept: Number[]}} `limited` is true when the in-window count is at/over\n *     the cap (the caller should refuse the call); `kept` is the pruned in-window timestamp list.\n */","meta":{"range":[927,1133],"filename":"askRateLimit.mjs","lineno":15,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/services/knowledge-base/helpers","code":{"id":"astnode100093217","name":"exports.checkAskRateLimit","type":"FunctionDeclaration","paramnames":["timestamps","now","maxPerMinute","windowMs"]}},"summary":"Pure rolling-window rate check for the `ask_knowledge_base` synthesis runaway breaker.\n\nLives in its own module so the breaker is unit-testable in isolation — importing it does NOT\nconstruct the `SearchService` singleton or read the shared `AiConfig` (no live-DB / config-bleed\nsurface). Generic + side-effect-free: callers pass the clock (`now`) and the cap explicitly.","params":[{"type":{"names":["Array.<Number>"]},"description":"<p>Prior call epoch-ms timestamps (the caller&#39;s rolling buffer).</p>\n","name":"timestamps"},{"type":{"names":["Number"]},"description":"<p>Current epoch-ms (injected for testability — never reads the clock itself).</p>\n","name":"now"},{"type":{"names":["Number"]},"description":"<p>Cap within the rolling window.</p>\n","name":"maxPerMinute"},{"type":{"names":["Number"]},"optional":true,"defaultvalue":60000,"description":"<p>Rolling window width in ms.</p>\n","name":"windowMs"}],"returns":[{"type":{"names":["Object"]},"description":"`limited` is true when the in-window count is at/over\n    the cap (the caller should refuse the call); `kept` is the pruned in-window timestamp list."}],"name":"checkAskRateLimit","longname":"checkAskRateLimit","kind":"function","scope":"global","$longname":"checkAskRateLimit","$kind":"method","id":9277,"neoClassName":"Neo.ai.services.knowledge-base.helpers.askRateLimit"}]}