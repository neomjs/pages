{"classData":[{"comment":"/**\n * @summary Subject B — the honest single-main-thread comparator for the Harness Endurance Benchmark.\n *\n * A dependency-free vanilla page: it runs the SAME deterministic `LoadProfile` stream as the Neo\n * subject, but parses (`markdownBlocks`) and renders ENTIRELY on the main thread — no workers. The\n * runner samples the SAME event-loop-lag metric here as for the Neo subject; the lag delta isolates\n * the worker-topology effect. The runner triggers a run via `globalThis.__enduranceComparator.start(cfg)`.\n *\n * RENDER (best-practice shape): tail-incremental + WINDOWED — settled blocks are written once then\n * frozen; only the open last block re-renders as it grows; and only the last `RENDER_WINDOW` blocks\n * stay mounted (older blocks evicted), so the DOM stays bounded at marathon scale — matching Neo's\n * MarkdownVdom virtualization. NOT an O(n²) full-`innerHTML` rewrite, and NOT an unbounded DOM:\n * windowing both subjects is what makes the lag delta isolate worker-topology rather than a\n * virtualization asymmetry (a non-virtualized comparator would conflate the two axes at scale).\n *\n * PARSE (best-practice shape): incremental — settled blocks (everything before the last blank line) are\n * parsed ONCE and only the open region re-parses from each delta (`createIncrementalBlocks`), mirroring\n * the off-thread Neo parser's memoization. So the lag delta isolates the worker-topology variable (WHERE\n * the work runs) rather than conflating it with parse strategy — the benchmark's naive-vs-best-practice\n * fork, resolved to best-practice.\n */","meta":{"range":[1712,1756],"filename":"comparator.mjs","lineno":27,"columnno":4,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/examples/harnessEndurance/comparator","code":{"id":"astnode100017893","name":"probe","type":"CallExpression","value":""}},"summary":"Subject B — the honest single-main-thread comparator for the Harness Endurance Benchmark.\n\nA dependency-free vanilla page: it runs the SAME deterministic `LoadProfile` stream as the Neo\nsubject, but parses (`markdownBlocks`) and renders ENTIRELY on the main thread — no workers. The\nrunner samples the SAME event-loop-lag metric here as for the Neo subject; the lag delta isolates\nthe worker-topology effect. The runner triggers a run via `globalThis.__enduranceComparator.start(cfg)`.\n\nRENDER (best-practice shape): tail-incremental + WINDOWED — settled blocks are written once then\nfrozen; only the open last block re-renders as it grows; and only the last `RENDER_WINDOW` blocks\nstay mounted (older blocks evicted), so the DOM stays bounded at marathon scale — matching Neo's\nMarkdownVdom virtualization. NOT an O(n²) full-`innerHTML` rewrite, and NOT an unbounded DOM:\nwindowing both subjects is what makes the lag delta isolate worker-topology rather than a\nvirtualization asymmetry (a non-virtualized comparator would conflate the two axes at scale).\n\nPARSE (best-practice shape): incremental — settled blocks (everything before the last blank line) are\nparsed ONCE and only the open region re-parses from each delta (`createIncrementalBlocks`), mirroring\nthe off-thread Neo parser's memoization. So the lag delta isolates the worker-topology variable (WHERE\nthe work runs) rather than conflating it with parse strategy — the benchmark's naive-vs-best-practice\nfork, resolved to best-practice.","name":"probe","longname":"probe","kind":"constant","scope":"global","params":[],"$longname":"probe","$kind":"constant","id":11607,"neoClassName":"Neo.ai.examples.harnessEndurance.comparator.comparator"},{"comment":"/**\n * Bounded DOM window. A best-practice main-thread renderer does NOT accumulate unbounded DOM at\n * marathon scale — it windows like a virtualized list. Only the last `RENDER_WINDOW` blocks stay\n * mounted (older evicted), matching Neo's MarkdownVdom virtualization so the lag delta isolates the\n * worker-topology variable (WHERE parse/render runs), not a virtualization asymmetry.\n * @type {Number}\n */","meta":{"range":[2427,2446],"filename":"comparator.mjs","lineno":44,"columnno":6,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/examples/harnessEndurance/comparator","code":{"id":"astnode100017941","name":"RENDER_WINDOW","type":"Literal","value":120}},"description":"<p>Bounded DOM window. A best-practice main-thread renderer does NOT accumulate unbounded DOM at\nmarathon scale — it windows like a virtualized list. Only the last <code>RENDER_WINDOW</code> blocks stay\nmounted (older evicted), matching Neo&#39;s MarkdownVdom virtualization so the lag delta isolates the\nworker-topology variable (WHERE parse/render runs), not a virtualization asymmetry.</p>\n","type":{"names":["Number"]},"name":"RENDER_WINDOW","longname":"RENDER_WINDOW","kind":"constant","scope":"global","params":[],"$longname":"RENDER_WINDOW","$kind":"constant","id":11608,"neoClassName":"Neo.ai.examples.harnessEndurance.comparator.comparator"},{"comment":"/**\n * Tail-incremental + WINDOWED apply: settled blocks (all but the last) are written ONCE then frozen;\n * only the open last block re-renders each tick as it grows; and only the last `RENDER_WINDOW` blocks\n * stay mounted (older evicted), so the DOM is bounded at marathon scale.\n * @param {String[]} blocks\n * @private\n */","meta":{"range":[2849,3748],"filename":"comparator.mjs","lineno":58,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/examples/harnessEndurance/comparator","code":{"id":"astnode100017955","name":"applyBlocks","type":"FunctionDeclaration","paramnames":["blocks"]},"vars":{"start":"applyBlocks~start","index":"applyBlocks~index","i":"applyBlocks~i","entry":"applyBlocks~entry","isOpenTail":"applyBlocks~isOpenTail","entry.el.innerHTML":"applyBlocks~entry.el.innerHTML","entry.settled":"applyBlocks~entry.settled"}},"description":"<p>Tail-incremental + WINDOWED apply: settled blocks (all but the last) are written ONCE then frozen;\nonly the open last block re-renders each tick as it grows; and only the last <code>RENDER_WINDOW</code> blocks\nstay mounted (older evicted), so the DOM is bounded at marathon scale.</p>\n","params":[{"type":{"names":["Array.<String>"]},"name":"blocks"}],"access":"private","name":"applyBlocks","longname":"applyBlocks","kind":"function","scope":"global","$longname":"applyBlocks","$kind":"method","id":11609,"neoClassName":"Neo.ai.examples.harnessEndurance.comparator.comparator"},{"comment":"/**\n * Drives the deterministic `LoadProfile` append stream, parsing the open block + rendering each delta\n * synchronously on the main thread (settled blocks memoized, DOM windowed). A run token prevents\n * overlapping runs. `getTotalChars()` reports the full accumulated transcript length (vs the windowed\n * DOM) so the runner can confirm the comparator also reached marathon scale.\n * @param {Object} [config] forwarded to `LoadProfile` (seed, durationMs, cadences).\n * @returns {Promise<void>}\n */","meta":{"range":[4253,4795],"filename":"comparator.mjs","lineno":98,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/examples/harnessEndurance/comparator","code":{"id":"astnode100018095","name":"start","type":"FunctionDeclaration","paramnames":["config"]},"vars":{"profile":"start~profile","token":"start~token","blocks":"start~blocks","":null,"totalChars":"start~totalChars","undefined":null}},"description":"<p>Drives the deterministic <code>LoadProfile</code> append stream, parsing the open block + rendering each delta\nsynchronously on the main thread (settled blocks memoized, DOM windowed). A run token prevents\noverlapping runs. <code>getTotalChars()</code> reports the full accumulated transcript length (vs the windowed\nDOM) so the runner can confirm the comparator also reached marathon scale.</p>\n","params":[{"type":{"names":["Object"]},"optional":true,"description":"<p>forwarded to <code>LoadProfile</code> (seed, durationMs, cadences).</p>\n","name":"config"}],"returns":[{"type":{"names":["Promise.<void>"]}}],"name":"start","longname":"start","kind":"function","scope":"global","async":true,"$longname":"start","$kind":"method","id":11610,"neoClassName":"Neo.ai.examples.harnessEndurance.comparator.comparator"}]}