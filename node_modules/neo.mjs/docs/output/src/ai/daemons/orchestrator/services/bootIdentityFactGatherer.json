{"classData":[{"comment":"/**\n * Live fact-gatherer factory for the boot-identity health surface — the integration layer the\n * advisory service injects. Reads the last scheduler cycle from the REM run-state store (the same\n * source the consolidation liveness watchdog reads) and pairs it with the process boot time so the\n * pure discriminator can classify a stale-wake.\n *\n * Conservative first cut: `schedulerResumeState` defaults to `'none'` (no scheduler re-arm signal\n * surface exists yet, so a boot-after-last-cycle conservatively reads as restart-explains), and\n * `deferralReason` / `sourceRef` are read best-effort (null when unavailable). Each is behind an\n * optional injected resolver, so the domain-specific derivations refine in place without restructuring.\n *\n * @param {Object}   options\n * @param {Number}   options.bootAt                          Epoch-ms captured at process/service construction.\n * @param {Function} options.readRecentRemRunStates          Async `({dir, limit}) => Promise<Object[]>` reader.\n * @param {String}   options.remRunStateDir                  Directory holding the REM run-state JSONL artifacts.\n * @param {Function} [options.resolveSourceRef]              `() => (String|null)` advisory source ref (never gitHead-primary).\n * @param {Function} [options.resolveDeferralReason]         `async () => (String|null)` why the scheduler deferred.\n * @param {Function} [options.resolveSchedulerResumeState]   `() => String` refines the conservative `'none'` default.\n * @returns {Function} async `() => {bootAt, sourceRef, schedulerResumeState, lastCycleAt, lastCycleRef, deferralReason}`\n */","meta":{"range":[1679,3327],"filename":"bootIdentityFactGatherer.mjs","lineno":23,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/daemons/orchestrator/services","code":{"id":"astnode100127829","name":"exports.createBootIdentityFactGatherer","type":"FunctionDeclaration","paramnames":[""]}},"description":"<p>Live fact-gatherer factory for the boot-identity health surface — the integration layer the\nadvisory service injects. Reads the last scheduler cycle from the REM run-state store (the same\nsource the consolidation liveness watchdog reads) and pairs it with the process boot time so the\npure discriminator can classify a stale-wake.</p>\n<p>Conservative first cut: <code>schedulerResumeState</code> defaults to <code>&#39;none&#39;</code> (no scheduler re-arm signal\nsurface exists yet, so a boot-after-last-cycle conservatively reads as restart-explains), and\n<code>deferralReason</code> / <code>sourceRef</code> are read best-effort (null when unavailable). Each is behind an\noptional injected resolver, so the domain-specific derivations refine in place without restructuring.</p>\n","params":[{"type":{"names":["Object"]},"name":"options"},{"type":{"names":["Number"]},"description":"<p>Epoch-ms captured at process/service construction.</p>\n","name":"options.bootAt"},{"type":{"names":["function"]},"description":"<p>Async <code>({dir, limit}) =&gt; Promise&lt;Object[]&gt;</code> reader.</p>\n","name":"options.readRecentRemRunStates"},{"type":{"names":["String"]},"description":"<p>Directory holding the REM run-state JSONL artifacts.</p>\n","name":"options.remRunStateDir"},{"type":{"names":["function"]},"optional":true,"description":"<p><code>() =&gt; (String|null)</code> advisory source ref (never gitHead-primary).</p>\n","name":"options.resolveSourceRef"},{"type":{"names":["function"]},"optional":true,"description":"<p><code>async () =&gt; (String|null)</code> why the scheduler deferred.</p>\n","name":"options.resolveDeferralReason"},{"type":{"names":["function"]},"optional":true,"description":"<p><code>() =&gt; String</code> refines the conservative <code>&#39;none&#39;</code> default.</p>\n","name":"options.resolveSchedulerResumeState"}],"returns":[{"type":{"names":["function"]},"description":"async `() => {bootAt, sourceRef, schedulerResumeState, lastCycleAt, lastCycleRef, deferralReason}`"}],"name":"createBootIdentityFactGatherer","longname":"createBootIdentityFactGatherer","kind":"function","scope":"global","$longname":"createBootIdentityFactGatherer","$kind":"method","id":10650,"neoClassName":"Neo.ai.daemons.orchestrator.services.bootIdentityFactGatherer"}]}