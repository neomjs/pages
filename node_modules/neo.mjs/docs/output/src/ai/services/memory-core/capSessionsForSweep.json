{"classData":[{"comment":"/**\n * @module ai/services/memory-core/capSessionsForSweep\n * @summary Bounds how many sessions a single summary sweep drains before the child exits and\n * releases the heavy-maintenance lease.\n *\n * `SessionService.summarizeSessions` otherwise drains the entire drift list in one child run, holding\n * the exclusive heavy-maintenance lease for the whole batch — starving `dream` / `backup` /\n * `memory-summary-backfill`, which only win the lease at a release boundary (the fair picker, see\n * `learn/agentos/decisions/0022-heavy-maintenance-scheduling-fairness.md`). Capping the per-sweep\n * count yields release boundaries frequently, so the picker interleaves the rest of the REM chain.\n *\n * The drift sweep is self-continuing — the next sweep re-derives the remaining sessions — so capping\n * CHUNKS the backlog, it never drops work.\n */","meta":{"filename":"capSessionsForSweep.mjs","lineno":1,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/services/memory-core","code":{}},"kind":"module","name":"ai/services/memory-core/capSessionsForSweep","summary":"Bounds how many sessions a single summary sweep drains before the child exits and\nreleases the heavy-maintenance lease.\n\n`SessionService.summarizeSessions` otherwise drains the entire drift list in one child run, holding\nthe exclusive heavy-maintenance lease for the whole batch — starving `dream` / `backup` /\n`memory-summary-backfill`, which only win the lease at a release boundary (the fair picker, see\n`learn/agentos/decisions/0022-heavy-maintenance-scheduling-fairness.md`). Capping the per-sweep\ncount yields release boundaries frequently, so the picker interleaves the rest of the REM chain.\n\nThe drift sweep is self-continuing — the next sweep re-derives the remaining sessions — so capping\nCHUNKS the backlog, it never drops work.","longname":"module:ai/services/memory-core/capSessionsForSweep","$longname":"ai/services/memory-core/capSessionsForSweep","$kind":"module","id":9874,"neoClassName":"Neo.ai.services.memory-core.capSessionsForSweep"},{"comment":"/**\n * @summary Caps a session list to its first `maxPerSweep` entries when that is a positive integer;\n * otherwise returns the list unchanged (an unset / zero / non-integer cap means \"no bound\").\n * Pure — no I/O, no `aiConfig` mutation — so it is unit-testable in isolation.\n * @param {Array} sessions The full drift list from `findSessionsToSummarize`.\n * @param {Number} maxPerSweep The per-sweep cap (read at the use site as `aiConfig.maxSessionsPerSummarySweep`).\n * @returns {Array} The capped (or unchanged) list.\n */","meta":{"range":[1372,1555],"filename":"capSessionsForSweep.mjs","lineno":24,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/services/memory-core","code":{"id":"astnode100079499","name":"exports.capSessionsForSweep","type":"FunctionDeclaration","paramnames":["sessions","maxPerSweep"]}},"summary":"Caps a session list to its first `maxPerSweep` entries when that is a positive integer;\notherwise returns the list unchanged (an unset / zero / non-integer cap means \"no bound\").\nPure — no I/O, no `aiConfig` mutation — so it is unit-testable in isolation.","params":[{"type":{"names":["Array"]},"description":"<p>The full drift list from <code>findSessionsToSummarize</code>.</p>\n","name":"sessions"},{"type":{"names":["Number"]},"description":"<p>The per-sweep cap (read at the use site as <code>aiConfig.maxSessionsPerSummarySweep</code>).</p>\n","name":"maxPerSweep"}],"returns":[{"type":{"names":["Array"]},"description":"The capped (or unchanged) list."}],"name":"capSessionsForSweep","longname":"module:ai/services/memory-core/capSessionsForSweep.capSessionsForSweep","kind":"function","memberof":"module:ai/services/memory-core/capSessionsForSweep","scope":"static","$longname":"ai/services/memory-core/capSessionsForSweep.capSessionsForSweep","$kind":"method","id":9875,"neoClassName":"Neo.ai.services.memory-core.capSessionsForSweep"}]}