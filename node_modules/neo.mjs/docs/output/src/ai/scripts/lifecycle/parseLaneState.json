{"classData":[{"comment":"/**\n * Pure parser that extracts the structured turn-terminal `lane-state` descriptor from an agent's\n * final transcript text, for the Stop-hook seam: `transcriptText → descriptor → validateLaneStateTerminal`.\n *\n * The emission convention is a fenced ```lane-state code block holding the descriptor as JSON (the\n * human-readable `lane-state:` prose line remains, but the MACHINE seam is the block). This parser is\n * deliberately continuation-set-agnostic — it extracts whatever the block declares; the lane-state\n * RULES live in `validateLaneStateTerminal`, so a change to the valid continuation set never touches\n * the parser.\n *\n * Three outcomes the hook distinguishes:\n *  - a well-formed block  → the descriptor object;\n *  - NO block present     → `null` (the \"absent emission\" case);\n *  - a block with broken JSON → it THROWS (a present-but-malformed emission is a distinct failure from absent).\n *\n * @module Neo.ai.scripts.lifecycle.parseLaneState\n */","meta":{"filename":"parseLaneState.mjs","lineno":1,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/lifecycle","code":{}},"description":"<p>Pure parser that extracts the structured turn-terminal <code>lane-state</code> descriptor from an agent&#39;s\nfinal transcript text, for the Stop-hook seam: <code>transcriptText → descriptor → validateLaneStateTerminal</code>.</p>\n<p>The emission convention is a fenced ```lane-state code block holding the descriptor as JSON (the\nhuman-readable <code>lane-state:</code> prose line remains, but the MACHINE seam is the block). This parser is\ndeliberately continuation-set-agnostic — it extracts whatever the block declares; the lane-state\nRULES live in <code>validateLaneStateTerminal</code>, so a change to the valid continuation set never touches\nthe parser.</p>\n<p>Three outcomes the hook distinguishes:</p>\n<ul>\n<li>a well-formed block  → the descriptor object;</li>\n<li>NO block present     → <code>null</code> (the &quot;absent emission&quot; case);</li>\n<li>a block with broken JSON → it THROWS (a present-but-malformed emission is a distinct failure from absent).</li>\n</ul>\n","kind":"module","name":"parseLaneState","memberof":"Neo.ai.scripts.lifecycle","longname":"Neo.ai.scripts.lifecycle.module:parseLaneState","scope":"static","$longname":"Neo.ai.scripts.lifecycle.module:parseLaneState","$kind":"module","id":8336,"neoClassName":"Neo.ai.scripts.lifecycle.parseLaneState"},{"comment":"/**\n * Matches a fenced ```lane-state block, capturing its inner body. Global so the LAST block wins\n * (an agent's true terminal claim is its final block, not an earlier illustrative one).\n * @type {RegExp}\n */","meta":{"range":[1181,1256],"filename":"parseLaneState.mjs","lineno":24,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/lifecycle","code":{"id":"astnode100076553","name":"exports.LANE_STATE_BLOCK","type":"VariableDeclaration"}},"description":"<p>Matches a fenced ```lane-state block, capturing its inner body. Global so the LAST block wins\n(an agent&#39;s true terminal claim is its final block, not an earlier illustrative one).</p>\n","type":{"names":["RegExp"]},"name":"LANE_STATE_BLOCK","longname":"Neo.ai.scripts.lifecycle.module:parseLaneState.LANE_STATE_BLOCK","kind":"constant","memberof":"Neo.ai.scripts.lifecycle.module:parseLaneState","scope":"static","$longname":"Neo.ai.scripts.lifecycle.module:parseLaneState.LANE_STATE_BLOCK","$kind":"constant","id":8337,"neoClassName":"Neo.ai.scripts.lifecycle.parseLaneState"},{"comment":"/**\n * @summary Parses the last fenced ```lane-state block from transcript text into a lane-state descriptor.\n *\n * @param {String} transcriptText The agent's final turn-terminal message text (or the transcript tail).\n * @returns {Object|null} The descriptor `{wakeDisposition, laneContinuation, namedGates, awaitingOwnPrOnly}`\n *   normalized from the LAST ```lane-state block, or `null` when no block is present.\n * @throws {Error} When a ```lane-state block IS present but its body is not valid JSON — a\n *   present-but-malformed emission is a distinct failure the hook logs separately from an absent one.\n */","meta":{"range":[1872,2686],"filename":"parseLaneState.mjs","lineno":35,"columnno":0,"path":"/Users/Shared/github/neomjs/pages/node_modules/neo.mjs/ai/scripts/lifecycle","code":{"id":"astnode100076558","name":"exports.parseLaneState","type":"FunctionDeclaration","paramnames":["transcriptText"]}},"summary":"Parses the last fenced ```lane-state block from transcript text into a lane-state descriptor.","params":[{"type":{"names":["String"]},"description":"<p>The agent&#39;s final turn-terminal message text (or the transcript tail).</p>\n","name":"transcriptText"}],"returns":[{"type":{"names":["Object","null"]},"description":"The descriptor `{wakeDisposition, laneContinuation, namedGates, awaitingOwnPrOnly}`\n  normalized from the LAST ```lane-state block, or `null` when no block is present."}],"exceptions":[{"type":{"names":["Error"]},"description":"When a ```lane-state block IS present but its body is not valid JSON — a\n  present-but-malformed emission is a distinct failure the hook logs separately from an absent one."}],"name":"parseLaneState","longname":"Neo.ai.scripts.lifecycle.module:parseLaneState.parseLaneState","kind":"function","memberof":"Neo.ai.scripts.lifecycle.module:parseLaneState","scope":"static","$longname":"Neo.ai.scripts.lifecycle.module:parseLaneState.parseLaneState","$kind":"method","id":8338,"neoClassName":"Neo.ai.scripts.lifecycle.parseLaneState"}]}