StartTest(function (t) {

    t.it('should always visit last point in the mousemove path', function (t) {
        t.chain(
            { moveCursorTo : [657, 123] },

            function () {
                t.isDeeply(t.currentPosition, [657, 123], 'Moved cursor to correct point');
            }
        )
    });
});
