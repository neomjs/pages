describe('Black box test', t => {
    t.chain(
        { click : '#root :textEquals(Compare)' },
        next => {
            t.selectorCountIs('#root :textEquals(Compare)', 2, 'Should open additional text box')
        }
    )
})