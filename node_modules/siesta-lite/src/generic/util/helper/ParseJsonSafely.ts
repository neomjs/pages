export const parseJsonSafely = (str : string, parseWith? : (str : string) => object) : object | undefined => {
    if (!parseWith) parseWith = JSON.parse

    let result

    try {
        result  = parseWith(str)
    } catch (e) {
        result  = undefined
    }

    return result
}
