StartTest(function(t) {

    var p = new Promise(function (resolve, reject) {
        reject();
    });

    p.then(function () {})

    // need to wait, because "unhandledrejection" event is async and test will be finalized before its fired
    t.waitFor(100)
})