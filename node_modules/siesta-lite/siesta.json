// default configuration file for launchers
{
    // default project-wide options
    project : {
        //forceDOMVisible     : true
    },

    // default command-line arguments
    cmd     : {
        //rerun-failed    : true
    }
}